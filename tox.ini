[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pandas
    wandb
    pytest-cov
commands = pytest --cov=my_project --cov-report=xml --cov-config=tox.ini --cov-branch -m "not data_check"
 
[coverage:run]